spring.application.name=backend

# DB
spring.datasource.url=${DB_URL:jdbc:postgresql://db.rgsqjpnsqlsaowxzqlzi.supabase.co:5432/postgres?prepareThreshold=0}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Shivang@1432}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.fetch_size=100
spring.jpa.properties.hibernate.query.in_clause_parameter_padding=true

# Add indexes for frequently queried columns (run these manually in Supabase)
# CREATE INDEX idx_product_category ON products(category);
# CREATE INDEX idx_product_name ON products(name);
# CREATE INDEX idx_product_price ON products(price);

# Connection Pool (HikariCP) - Optimized for Supabase with connection pooling
spring.datasource.hikari.maximum-pool-size=30
spring.datasource.hikari.minimum-idle=10
spring.datasource.hikari.connection-timeout=10000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.auto-commit=true
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.leak-detection-threshold=60000

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ultronthor037@gmail.com
spring.mail.password=fuuj adbe wozd wbce
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT
jwt.secret=8fK3!xP9@vQ2#Lm7$Zr6&Nd1*Ht5YwU4Bs0GkJcR
jwt.expiration=86400000

# Admin
admin.email=shivangv493@gmail.com

# Cache Configuration - Aggressive caching for products
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=1000,expireAfterWrite=300s,recordStats
spring.cache.cache-names=products,productList

# Server compression
server.compression.enabled=true
server.compression.min-response-size=1024
server.compression.mime-types=application/json,application/xml,text/html,text/xml,text/plain

# Performance
server.tomcat.threads.max=200
server.tomcat.threads.min-spare=10
server.tomcat.max-connections=10000

# Server binding
server.port=${PORT:8080}
server.address=0.0.0.0
server.servlet.context-path=/

# HTTP caching headers
server.http2.enabled=true

# Startup logging
logging.level.root=INFO
logging.level.org.springframework.boot.web=INFO

# Database query optimization
spring.jpa.properties.hibernate.generate_statistics=false
spring.jpa.properties.hibernate.use_sql_comments=false
spring.jpa.properties.hibernate.jdbc.fetch_size=100

# Keep DB fixer disabled in production-like environments.
# Enable manually only when needed via env: APP_DB_FIXER_ENABLED=true
app.db.fixer.enabled=${APP_DB_FIXER_ENABLED:false}

# Keep product seeding off in deployment by default.
# Enable manually only when needed via env: APP_SEED_PRODUCTS_ENABLED=true
app.seed.products.enabled=${APP_SEED_PRODUCTS_ENABLED:false}

server.port=${PORT:8080}
